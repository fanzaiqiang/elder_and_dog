# Face Dashboard Next.js Frontend

## Run

```bash
cd /home/jetson/elder_and_dog/face_dashboard_nextjs
cp .env.example .env.local
npm install
npm run dev
```

Open `http://localhost:3000`.

Set `NEXT_PUBLIC_API_BASE` in `.env.local` to your FastAPI URL.
